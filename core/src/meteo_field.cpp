// Translation-unit anchor for the header-only MeteoField hierarchy. Ensures the
// header compiles standalone and provides a home for future field
// implementations backed by gridded QGIS/GDAL data.
#include "nephele/meteo_field.hpp"

namespace nephele {

// MeteoField and UniformMeteoField are currently fully defined inline.

}  // namespace nephele
