// Translation-unit anchor for the header-only ConcentrationGrid. Keeping a
// dedicated source file ensures the header is compiled in isolation by the
// build and gives a stable place for future out-of-line definitions.
#include "nephele/concentration_grid.hpp"

namespace nephele {

// ConcentrationGrid is currently fully defined inline in the header.

}  // namespace nephele
