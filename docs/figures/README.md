# Figures

Place rendered figures here (plume snapshots, sensor-tasking animations,
benchmark plots). Generate a quick demo footprint with:

```bash
python scripts/run_scenario.py --config data/demo/release.yaml --output hazard.geojson
```

then visualise `hazard.geojson` in QGIS over `data/demo/buildings.geojson`.
